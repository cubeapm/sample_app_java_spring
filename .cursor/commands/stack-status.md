Show sample mesh status (HTTP probes on java/nodejs/python/php/dotnet + compose ps).

Run exactly:

```bash
./scripts/stack.sh status
```

Return the script summary only. Do not poll or invent docker commands.
